# Copier vers .env — doit être identique à ADMIN_API_TOKEN du bot (Bearer).
VITE_ADMIN_API_TOKEN=remplacez-moi

# Optionnel : cible du proxy Vite si le bot n’écoute pas sur 3840
# VITE_ADMIN_PROXY_TARGET=http://127.0.0.1:3840
